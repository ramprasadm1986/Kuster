/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Index from './js/config/router';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Index);
